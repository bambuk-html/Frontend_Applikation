/*import { useState } from "react";
import axios from 'axios';

export default function GetTodo() {
    const [tasks, setTasks] = useState([]);

    const jetData = () => {
        return axios.get("127.0.0.1:3000/tasks").then((response) => setTasks(response.data));
    }

    return(
    <div>
        <button id="get" name="holen" onClick={jetData} >Holen</button>
        <p>{tasks}</p>
    </div>
    )

}
*/

export{}